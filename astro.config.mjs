// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://zayaraung94.github.io",
  base: "/az_metertaxi_web/", // သင့် repository နာမည်ကို အတိအကျထည့်ပါ
  output: "static",
});
